version: "3.9"
services:
  web:
    build:
      context: ./apps/web
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped

  api:
    build:
      context: ./apps/api
    ports:
      - "3333:3333"
    env_file:
      - .env
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped
